package:
  name: shenfun
  version: "1.0.2"

source:
  git_url: ../../

build:
  number: 8

requirements:
  build:
    - python
    - cython
    - numpy
    - pip

  run:
    - python
    - numpy
    - scipy
    - cython
    - pyfftw 0.10.0
    - mpi4py
    - six
    - sympy
    - h5py-parallel

test:
  source_files:
    - tests
    - demo

  imports:
    - shenfun

  requires:
    - pytest
