package:
  name: shenfun
  version: "{{ GIT_VERSION_TAG }}"

source:
  git_url: ../../

build:
  number: 0

requirements:
  build:
    - python
    - cython
    - numpy
    - pip
    - fftw
    - nomkl

  run:
    - python
    - numpy
    - scipy
    - cython
    - fftw
    - mpi4py
    - mpi4py_fft
    - sympy
    - nomkl
    - h5py-parallel
    - libnetcdf-parallel
    - netcdf4-parallel

test:
  source_files:
    - tests
    - demo

  imports:
    - shenfun

  requires:
    - pytest
