package:
  name: shenfun
  version: "1.0"

source:
  git_url: ../../

build:
  number: 1
  script: python setup.py install --prefix=$PREFIX

requirements:
  build:
    - python
    - cython
    - numpy

  run:
    - python
    - numpy
    - scipy
    - mpifft4py
    - cython
    - pyfftw
    - six
    - sympy

test:
  source_files:
    - tests

  requires:
    - pytest
